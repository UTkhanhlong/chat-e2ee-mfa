import { Router } from 'express'; import { authGuard } from '../../middlewares/authGuard'; import { asyncWrap } from '../../common/errors'; import { MfaController } from './mfa.controller'; const r=Router(); r.post('/totp/setup',authGuard,asyncWrap(MfaController.totpSetup)); r.post('/totp/verify',authGuard,asyncWrap(MfaController.totpVerify)); r.post('/totp/login',authGuard,asyncWrap(MfaController.totpLogin)); export default r;
